# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Cargo (workspace target)
/target

# Эталонная копия клиента (~7 ГБ) — НЕ в git
/master-client
/dist-manifest
/cas-stage

# Секреты — НИКОГДА не коммитить
*.key
keys/
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# payload клиента (раздаётся через master-client/manifest, не исходник лаунчера)
/_launcher/

# Исходные архивы клиента (не исходник лаунчера)
/L2_Client_Release.zip
/client-staging/
