# Статус 2026-07-11 — миграция раздачи на R2 + аудит безопасности

Рабочая заметка/handoff. Что сделано в этой сессии и что осталось открытым (продолжить позже).

## ✅ Сделано и проверено

### Раздача клиента переехала на Cloudflare R2 (сервер выведен)
- Новый клиент (сырой, с Google Drive) распакован, перенесён `_launcher/`, вычищены
  excluded (`d3d8.dll`/`dgVoodoo.conf`) и состояние игрока.
- Манифест **`2026.07.11.0138`** (1544 файла, zstd-раздача 2.65 ГБ), подпись Ed25519
  сверена с вшитым `MANIFEST_PUBKEY` — валидна.
- Клиент залит в **R2 бакет `master-client` под `c/2026.07.11.0138/`** (rclone, `no_check_bucket=true`).
  Сверка 1544/1544, spot-check sha256 (`L2.exe`) совпал.
- `l2files.balabanets.uk` переключён с тоннеля (nginx) на **custom domain R2** (ssl/ownership active).
  Отдача проверена: 200, Range 206, hash совпал, `cf-cache: DYNAMIC` = origin R2.
- Манифест опубликован на GitHub-релиз `manifest` (`Balabanets/l2-client`) — живой, версия совпадает.

### Вывод сервера и чистка
- Контейнер `l2-cdn` (nginx) удалён; `l2files`-ingress убран из тоннеля `cloudflared-lineage2`
  (остался только `l2.balabanets.uk`). Раздача с домашнего сервера прекращена.
- Старая серверная копия `master-client` заменена локальным build-reference (не раздаётся, gitignored).
- Орфан-префикс `client/` в R2 удалён; тестовые артефакты вычищены; бэкапы/staging убраны.
- `l2-cdn` не воскреснет (нет systemd-юнита/compose), порт 8091 свободен.

### Баг l2site (вложения тикетов) исправлен
- `l2site/.env`: `R2_BUCKET` `Tickets → l2-tickets`, `R2_ENDPOINT` плейсхолдер → реальный
  (`https://0f5793a6…r2.cloudflarestorage.com`). Контейнер `l2site` перезапущен, сайт + Слой 2 → 200.
  Запись в `l2-tickets` кредами сайта проверена.

### Токены
- Роллить не нужно — **тестовая среда** (подтверждено владельцем). CF API-токен «claude» (user-owned,
  полные права R2/DNS) в `~/.config/l2-launcher/keys/cf_api_token` (600). R2-ключ записи — в rclone `r2:`.

## 🔓 Аудит безопасности «в игру только через лаунчер» — ОТКРЫТЫЕ ПУНКТЫ

Механизмы реализованы корректно (подпись манифеста, HMAC + одноразовый nonce TTL 30с, HWID,
timing-safe, секреты не в гите/дефолтах, CI рвёт сборку без `LAUNCHER_HMAC_SECRET`, path-traversal
закрыт и покрыт тестами). Но:

### 🔴 CRITICAL — не подтверждено кодом: реально ли aCis зовёт `/api/launcher/check`
Вся защита держится на том, что игровой сервер aCis при логине дёргает `check` и работает
**fail-closed**. Исходников aCis на машине нет; `docs/ACIS_INTEGRATION.md` — заготовка-промпт, а не
подтверждение (в нём прямо: «пока aCis не зовёт check — любой запуск проходит»).
**TODO:** подтвердить на стороне aCis: (1) вызов `check` в точке входа игрока есть; (2) политика
fail-closed; (3) протестировано «запуск `L2.exe` напрямую, мимо лаунчера → отказ входа».

### 🟠 HIGH — `LAUNCHER_REQUIRE_LOGIN` выключен в проде
Сейчас `authorize` авторизует IP **без сессионного токена** → кто угодно со знанием HMAC-секрета и
хешей файлов может авторизоваться без входа через сайт и игрового аккаунта. Официальный лаунчер токен
шлёт всегда (`session.rs:144`, play требует входа `lib.rs:488`), поэтому включение безопасно.
**TODO (решение владельца):** `LAUNCHER_REQUIRE_LOGIN=1` в `l2site/.env` + рестарт `l2site`. Обратимо.

### Мелочи (LOW)
- `.u`-паки и файлы вне `system/` не входят в «критичные» для Слоя 2 (дёшево добавить `system/*.u`).
- Авторизация/nonce/rate-limit — in-memory Map (сброс при рестарте l2site; развалится при 2+ инстансах).
- `docs/ACIS_INTEGRATION.md` устарел по TTL: код — 15 мин / 10 логинов (в доке 120с / 4).

## Ключевые файлы (для продолжения)
- Слой 2 лаунчер: `src-tauri/src/session.rs`, `lib.rs` (authorize/heartbeat), `verify.rs`, `manifest.rs`
- Бэкенд: `l2site/src/app/api/launcher/{challenge,authorize,check}/route.ts`, `l2site/src/lib/launcher-auth.ts`
- aCis-интеграция (заготовка): `docs/ACIS_INTEGRATION.md`
- Раздача/операции R2: см. `CLAUDE.md` §6–§7
